{
  "name": "@seos/security",
  "version": "0.1.0",
  "type": "module",
  "bin": { "seos-security": "dist/index.js" },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts src/register.ts --format esm --dts --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@seos/context": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
